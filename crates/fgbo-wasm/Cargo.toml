[package]
name = "fgbo-wasm"
description = "FGBO reader for the browser: range-driven decoding + MVT tile rendering"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
fgbo = { path = "../fgbo", default-features = false }
wasm-bindgen = "0.2"
